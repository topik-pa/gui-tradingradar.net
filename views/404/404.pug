extends ../layout.pug

block append meta
  title #{__(title)} | #{proj_name}
block main
  main 
    section 
      h1 404
      p La pagina che stai cercando sembra non essere disponibile oppure è stata rimossa.#[br]Vai alla #[a(href=proj_url, title="Home page") Home page] per ricominciare la tua navigazione. #[br]#[br] Oppure...
      
      include ../../components/index/stocks_list/stocks_list.pug
      
      a(href=proj_url, title="Home page") #{proj_name}